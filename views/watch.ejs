<section class="watch">
  <h1 class="title"><%= v.title %></h1>

  <div class="player-card glow">
    <div class="ad-slot">[ Zona Iklan Atas Player ]</div>
    <div class="player">
      <video controls playsinline preload="metadata" src="<%= v.url %>"></video>
    </div>

    <div class="cta-wrap">
      <% if (v.fullUrl) { %>
        <a class="btn btn-primary" href="<%= v.fullUrl %>" target="_blank" rel="noopener">▶ Tonton FULL</a>
      <% } else { %>
        <div class="muted">Link FULL belum diisi.</div>
      <% } %>
    </div>
  </div>

  <div class="meta">
    <p><%= v.description || '' %></p>
  </div>
</section>
