<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= (site && site.title) || "Video Portal" %></title>
  <meta name="description" content="<%= (site && site.description) || 'Trailer video + tonton full lewat Telegram' %>">
  <link rel="canonical" href="<%= site?.canonical || (site?.baseUrl || '') %>">
  <meta property="og:title" content="<%= site?.og?.title || site?.title || 'Video Portal' %>">
  <meta property="og:description" content="<%= site?.og?.description || site?.description || '' %>">
  <meta property="og:url" content="<%= site?.og?.url || site?.canonical || '' %>">
  <link rel="stylesheet" href="/public/styles.css">
</head>
<body class="page-<%= page || 'home' %>">
  <header class="site-header">
    <div class="container">
      <a href="/" class="brand">🎬 <span>Video Portal</span></a>
    </div>
  </header>

  <main class="container">
    <%- body %>
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>© <%= new Date().getFullYear() %> Video Portal</p>
    </div>
  </footer>
</body>
</html>
